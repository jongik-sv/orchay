# PRD: orchay - AI 기반 프로젝트 관리 도구 (1차)

## 문서 정보

| 항목 | 내용 |
|------|------|
| 문서 버전 | 1.4 |
| 작성일 | 2025-12-18 |
| 범위 | 1차 (WBS 트리 뷰 중심) |
| 상태 | Draft |

---

## 1. 제품 개요

### 1.1 제품 비전

**"orchay - LLM과 함께 개발하는 차세대 프로젝트 관리 도구"**

개발자의 로컬 환경에서 실행되는 경량 프로젝트 관리 도구. 파일 기반 데이터 저장으로 Git 친화적이며, LLM CLI가 직접 마크다운 파일을 수정할 수 있어 AI와의 협업이 자연스럽습니다.

### 1.2 핵심 특징

- **로컬 실행**: `npx orchay`으로 바로 실행
- **파일 기반 데이터**: wbs.md + JSON으로 Git 친화적
- **Git 동기화**: Git push/pull이 곧 백업/동기화
- **LLM CLI 통합**: Claude Code 등이 직접 마크다운 수정 가능
- **설치 불필요**: npm 설치 없이 npx로 즉시 실행

### 1.3 타겟 사용자

- **주 사용자**: 소규모 개발팀 (1-10명)
- **환경**: 로컬 개발 환경
- **사용 시나리오**:
  - Git 기반 워크플로우 선호
  - AI 기반 코드 작성 및 리뷰
  - 자동화된 설계 문서 생성

---

## 2. 1차 범위

### 2.1 포함 기능

| 기능 | 설명 |
|------|------|
| WBS 트리 뷰 | 계층 구조 시각화, 확장/축소, 검색, 요약 카드 |
| Task 상세 패널 | 기본 정보, 상태, 워크플로우, 문서 표시 |
| 워크플로우 엔진 | 상태 전이, 카테고리별 규칙 |
| 데이터 저장소 | wbs.md 파싱/저장, JSON 설정 관리 |
| 프로젝트 초기화 | .orchay 폴더 구조 생성 |

### 2.2 제외 기능 (2차)

- 대시보드
- 칸반 보드
- Gantt 차트
- 터미널 통합 (xterm.js)

---

## 3. 기술 스택

| 계층 | 기술 | 버전 |
|------|------|------|
| 런타임 | Node.js | 20.x |
| 프레임워크 | Nuxt 3 | Standalone 모드 |
| 프론트엔드 | Vue 3 | Composition API |
| UI 컴포넌트 | PrimeVue | 4.x |
| 스타일링 | TailwindCSS | 3.x |
| 데이터 | wbs.md + JSON | 파일 기반 |

---

## 4. WBS 계층 구조

### 4.1 계층 구조

| 레벨 | 명칭 | 설명 | 기간 |
|------|------|------|------|
| Level 1 | Project | 전체 프로젝트 | 6~24개월 |
| Level 2 | Work Package | 주요 기능 단위의 작업 묶음 | 1~3개월 |
| Level 3 | Activity | 세부 활동 단위 (선택) | 1~4주 |
| Level 4 | Task | 실제 수행 작업 단위 | 1일~1주 |

### 4.2 계층 다이어그램

**4단계 구조 (대규모)**
```
Project
├── Work Package #1
│   ├── Activity #1.1
│   │   ├── Task #1.1.1
│   │   └── Task #1.1.2
│   └── Activity #1.2
│       └── Task #1.2.1
└── Work Package #2
    └── Activity #2.1
```

**3단계 구조 (소규모)**
```
Project
├── Work Package #1
│   ├── Task #1.1
│   └── Task #1.2
└── Work Package #2
    └── Task #2.1
```

### 4.3 Task 카테고리

| 카테고리 | 코드 | 설명 |
|---------|------|------|
| development | dev | 신규 기능 개발 작업 |
| defect | defect | 결함 수정 작업 |
| infrastructure | infra | 인프라, 리팩토링 등 기술 작업 |

---

## 5. 워크플로우 시스템

### 5.1 칸반 컬럼 (7단계)

```
┌────────┬──────────┬──────────┬──────────┬────────────┬──────────┬────────┐
│  Todo  │  Design  │  Detail  │ Approve  │ Implement  │  Verify  │  Done  │
│  [ ]   │   [bd]   │   [dd]   │   [ap]   │    [im]    │   [vf]   │  [xx]  │
├────────┼──────────┼──────────┼──────────┼────────────┼──────────┼────────┤
│  대기   │ 기본설계  │ 상세분석  │ 설계승인  │  구현/수정  │   검증    │  완료   │
└────────┴──────────┴──────────┴──────────┴────────────┴──────────┴────────┘
```

### 5.2 카테고리별 상태 흐름

**development**
```
[ ] → [bd] → [dd] → [ap] → [im] → [vf] → [xx]
```

**defect**
```
[ ] → [an] → [fx] → [vf] → [xx]
```

**infrastructure**
```
[ ] → [dd]? → [im] → [xx]
```

### 5.3 워크플로우 명령어

**development 카테고리**

| 명령어 | 현재 상태 | 다음 상태 | 설명 | 생성 문서 |
|--------|----------|----------|------|----------|
| `start` | `[ ]` | `[bd]` | 기본설계 시작 | `010-basic-design.md` |
| `draft` | `[bd]` | `[dd]` | 상세설계 시작 | `020-detail-design.md` |
| `approve` | `[dd]` | `[ap]` | 설계승인 | - |
| `build` | `[ap]` | `[im]` | 구현 시작 | `030-implementation.md` |
| `verify` | `[im]` | `[vf]` | 통합테스트 시작 | `070-integration-test.md` |
| `done` | `[vf]` | `[xx]` | 작업 완료 | `080-manual.md` |

**상태 내 액션 (상태 변경 없음)**

| 명령어 | 사용 가능 상태 | 설명 | 생성 문서 |
|--------|---------------|------|----------|
| `ui` | `[bd]` | 화면설계 작성 | `011-ui-design.md` |
| `review` | `[dd]` | LLM 설계 리뷰 | `021-design-review-{llm}-{n}.md` |
| `apply` | `[dd]` | 리뷰 내용 반영 | - |
| `test` | `[im]` | TDD/E2E 테스트 | `070-tdd-test-results.md` |
| `audit` | `[im]` | LLM 코드 리뷰 | `031-code-review-{llm}-{n}.md` |
| `patch` | `[im]` | 리뷰 내용 반영 | - |

---

## 6. 화면 설계

### 6.1 레이아웃 구조

```
┌─────────────────────────────────────────────────────────────────┐
│ Header: orchay | 대시보드 | 칸반 | WBS | Gantt | 프로젝트명      │
├─────────────────────────────────────┬───────────────────────────┤
│ WBS Tree Panel (60%)                │ Detail Panel (40%)        │
│ ┌─────────────────────────────────┐ │ ┌─────────────────────┐   │
│ │ 📁 WBS 트리 | 🔍검색 | 펼침/접기 │ │ │ TSK-08-01           │   │
│ │ [WP:11] [ACT:2] [TSK:50] [0%]   │ │ │ 디렉토리 구조 초기화  │   │
│ └─────────────────────────────────┘ │ │ 상태: [bd]            │   │
│                                     │ ├─────────────────────┤   │
│ ▼ P orchay - AI 기반 프로젝트      │ │ 기본 정보             │   │
│   ▼ WP WP-08: Data & Storage  25%  │ │ - 카테고리: infra     │   │
│     ● T TSK-08-01 [bd] infra       │ │ - 우선순위: 높음       │   │
│     ○ T TSK-08-02 [dd] dev         │ │ - 담당자: Lead Member │   │
│     ○ T TSK-08-03 [dd] dev         │ ├─────────────────────┤   │
│   ▶ WP WP-01: Issue Management     │ │ 진행 상태             │   │
│     ▶ A ACT-01-01: Entity CRUD     │ │ [ ]→[bd]→[dd]→[im]    │   │
│   ▶ WP WP-02: Workflow Engine      │ ├─────────────────────┤   │
│                                     │ │ 요구사항              │   │
│                                     │ │ • .orchay/ 폴더 생성  │   │
│                                     │ ├─────────────────────┤   │
│                                     │ │ 관련 문서             │   │
│                                     │ │ 📄 010-basic-design  │   │
│                                     │ ├─────────────────────┤   │
│                                     │ │ [편집] [문서] [draft] │   │
│                                     │ └─────────────────────┘   │
└─────────────────────────────────────┴───────────────────────────┘
```

### 6.2 WBS 트리 패널

#### 6.2.1 헤더 영역

| 요소 | 설명 |
|------|------|
| 아이콘 + 제목 | 📁 WBS 트리 |
| 검색 박스 | Task ID 또는 제목으로 필터링 |
| 펼치기/접기 | 전체 트리 확장/축소 |
| 요약 카드 | WP/ACT/TSK 수, 전체 진행률 |

#### 6.2.2 트리 노드 표시

```
[펼침] [아이콘] [제목] [진행률] [Task수] [상태태그]
```

| 요소 | 설명 |
|------|------|
| 펼침 아이콘 | ▼ 펼침 / ▶ 접힘 |
| 레벨 아이콘 | P(보라) / WP(파랑) / A(초록) / T(주황) |
| 제목 | ID + 이름 |
| 진행률 | 프로그레스 바 (%) |
| Task 수 | 하위 Task 개수 |
| 상태 태그 | 카테고리(dev/infra), 상태([bd]) |

#### 6.2.3 상호작용

- **클릭**: 노드 선택 → 상세 패널 표시
- **더블클릭**: 펼침/접힘 토글
- **키보드**: 화살표 탐색, Enter 선택

### 6.3 상세 패널

#### 6.3.1 기본 정보 섹션

| 필드 | 설명 |
|------|------|
| Task ID | TSK-{wp}-{nn} 형식 |
| 제목 | Task 이름 |
| 카테고리 | dev / defect / infra (태그) |
| 우선순위 | 높음(빨강) / 보통(노랑) / 낮음(초록) |
| 담당자 | 아바타 + 이름 |
| 상위 WP | 소속 Work Package |

#### 6.3.2 진행 상태 섹션

- **현재 상태**: 상태명 + 설명
- **워크플로우 시각화**:
```
[ ] → [bd] → [dd] → [im] → [xx]
      ^^^^
      현재
```

#### 6.3.3 요구사항 섹션

- 마크다운 형식 요구사항 목록
- PRD 섹션 참조 링크

#### 6.3.4 관련 문서 섹션

| 문서 | 상태 |
|------|------|
| 010-basic-design.md | 존재함 (파란 배경) |
| 020-detail-design.md | 예정 (점선 테두리) |

#### 6.3.5 작업 버튼

| 버튼 | 동작 |
|------|------|
| 편집 | Task 정보 수정 모달 |
| 문서 열기 | 현재 단계 문서 표시 |
| [다음상태] | 워크플로우 상태 전이 (예: draft) |

### 6.5 다중 프로젝트 통합 뷰

#### 6.5.1 전체 프로젝트 트리

- **URL**: `/wbs` (project 파라미터 없이)
- 모든 프로젝트를 최상위 노드로 표시
- 계층: 프로젝트 → WP → ACT → TSK
- 기본 상태: 프로젝트 노드만 펼침 (WP 목록 표시)
- `?project=xxx` 파라미터 시 기존 단일 프로젝트 뷰 유지

#### 6.5.2 프로젝트 상세 패널

프로젝트 노드 클릭 시 표시:

| 항목 | 설명 |
|------|------|
| 프로젝트명 | project.json의 name |
| 설명 | project.json의 description |
| 일정 | scheduledStart ~ scheduledEnd |
| 팀원 목록 | team.json 기반 |
| WBS 깊이 | 3단계 / 4단계 |
| 전체 진행률 | WP 평균 |

#### 6.5.3 프로젝트 파일 목록

프로젝트 폴더 내 파일 목록 표시:

| 파일 타입 | 뷰어 |
|----------|------|
| `.md` | 마크다운 렌더링 (기존 방식) |
| 이미지 (`.png`, `.jpg`, `.gif`) | 이미지 표시 |
| 기타 (`.json`, `.ts`) | Monaco Editor (raw 형식) |

---

## 7. 데이터 구조

### 7.1 디렉토리 구조

```
.orchay/
├── settings/                      # 전역 설정
│   ├── projects.json              # 프로젝트 목록
│   ├── columns.json               # 칸반 컬럼 정의
│   ├── categories.json            # 카테고리 정의
│   ├── workflows.json             # 워크플로우 규칙
│   └── actions.json               # 상태 내 액션 정의
│
├── templates/                     # 문서 템플릿
│   ├── 010-basic-design.md
│   ├── 020-detail-design.md
│   └── ...
│
└── projects/                      # 프로젝트 폴더
    └── [project-id]/
        ├── project.json           # 프로젝트 메타데이터
        ├── team.json              # 팀원 목록
        ├── wbs.md                 # WBS 통합 파일 (유일한 소스)
        └── tasks/                 # Task 문서 폴더
            └── {TSK-ID}/
                ├── 010-basic-design.md
                └── 020-detail-design.md
```

### 7.2 wbs.md 형식

```markdown
# WBS - ORCHAY Project Manager

> version: 1.0
> depth: 4
> updated: 2025-12-13

---

## WP-08: Data & Storage
- status: in_progress
- priority: high
- schedule: 2025-01-15 ~ 2025-02-14
- progress: 25%

### TSK-08-01: 디렉토리 구조 초기화
- category: infrastructure
- status: [bd]
- priority: high
- assignee: lead-member
- schedule: 2025-01-15 ~ 2025-01-21
- tags: backend, init
- requirements:
  - .orchay/ 폴더 구조 생성
  - project.json, team.json 초기화
  - Backend: InitService 구현
- ref: PRD 5.1
- completed:
  - ds: 2025-01-15 09:30

### TSK-08-02: JSON 파일 CRUD 서비스
- category: development
- status: [dd]
- priority: medium
- assignee: backend-dev
```

### 7.3 wbs.md 문법 규칙

| 레벨 | 마크다운 | ID 패턴 |
|------|----------|---------|
| WP | `## WP-XX:` | `WP-{2자리}` |
| ACT (4단계) | `### ACT-XX-XX:` | `ACT-{WP}-{순번}` |
| TSK (4단계) | `#### TSK-XX-XX-XX:` | `TSK-{WP}-{ACT}-{순번}` |
| TSK (3단계) | `### TSK-XX-XX:` | `TSK-{WP}-{순번}` |

### 7.4 Task 속성

| 속성 | 필수 | 설명 |
|------|------|------|
| category | O | `development`, `defect`, `infrastructure` |
| status | O | `[ ]`, `[bd]`, `[dd]`, `[im]`, `[vf]`, `[xx]` |
| priority | O | `critical`, `high`, `medium`, `low` |
| assignee | - | 담당자 ID 또는 `-` |
| schedule | - | `YYYY-MM-DD ~ YYYY-MM-DD` |
| tags | - | 쉼표로 구분된 태그 목록 |
| depends | - | 선행 Task ID |
| requirements | - | 요구사항 목록 |
| ref | - | 참조 문서 |
| completed | - | 워크플로우 단계별 완료시각 (아래 형식) |

### 7.5 completed 필드 형식

각 워크플로우 단계 완료 시 자동 기록되는 타임스탬프입니다.

**형식:**
```markdown
- completed:
  - bd: 2025-12-15 10:30
  - dd: 2025-12-15 14:20
  - im: 2025-12-16 09:15
```

**카테고리별 기록 항목:**

| 카테고리 | 기록 단계 |
|---------|----------|
| development | `bd`, `dd`, `im`, `vf`, `xx` |
| defect | `an`, `fx`, `vf`, `xx` |
| infrastructure | `ds`, `im`, `xx` |

**동작:**
- 상태 전이(transition) API 호출 시 자동 기록
- 시간 형식: `YYYY-MM-DD HH:mm` (로컬 시간)
- 이전 단계로 롤백 시 해당 단계 이후 기록 삭제

---

## 8. API 설계

### 8.1 Server Routes (Nuxt)

| Method | Route | 설명 |
|--------|-------|------|
| GET | /api/projects | 프로젝트 목록 |
| GET | /api/projects/:id | 프로젝트 상세 |
| POST | /api/projects | 프로젝트 생성 |
| GET | /api/projects/:id/wbs | WBS 트리 데이터 |
| PUT | /api/projects/:id/wbs | WBS 저장 |
| GET | /api/tasks/:id | Task 상세 |
| PUT | /api/tasks/:id | Task 수정 |
| POST | /api/tasks/:id/transition | 상태 전이 |
| GET | /api/tasks/:id/documents | Task 문서 목록 |
| GET | /api/settings/:type | 설정 조회 |

### 8.2 WBS 트리 응답 구조

```typescript
interface WbsNode {
  id: string;           // "WP-08" | "TSK-08-01"
  type: 'project' | 'wp' | 'act' | 'task';
  title: string;
  status?: string;      // "[bd]" | "[dd]" | ...
  category?: string;    // "dev" | "infra" | "defect"
  progress: number;     // 0-100
  taskCount: number;
  children: WbsNode[];
  expanded?: boolean;
}

interface TaskDetail {
  id: string;
  title: string;
  category: string;
  status: string;
  priority: string;
  assignee?: TeamMember;
  parentWp: string;
  requirements: string[];
  ref?: string;
  documents: DocumentInfo[];
  availableActions: string[];  // 현재 상태에서 가능한 명령어
}
```

---

## 9. 컴포넌트 구조

### 9.1 페이지

```
pages/
├── index.vue          # 리다이렉트 → /wbs
└── wbs.vue            # WBS 트리 뷰
```

### 9.2 컴포넌트

```
components/
├── layout/
│   ├── AppHeader.vue
│   └── AppLayout.vue
├── wbs/
│   ├── WbsTreePanel.vue      # 트리 패널 컨테이너
│   ├── WbsTreeNode.vue       # 트리 노드 (재귀)
│   ├── WbsTreeHeader.vue     # 헤더 (검색, 펼침/접기, 요약)
│   ├── WbsSearchBox.vue      # 검색 입력
│   └── WbsSummaryCards.vue   # 요약 카드들
├── detail/
│   ├── TaskDetailPanel.vue   # 상세 패널 컨테이너
│   ├── TaskBasicInfo.vue     # 기본 정보 섹션
│   ├── TaskProgress.vue      # 진행 상태 섹션
│   ├── TaskWorkflow.vue      # 워크플로우 시각화
│   ├── TaskRequirements.vue  # 요구사항 섹션
│   ├── TaskDocuments.vue     # 관련 문서 섹션
│   └── TaskActions.vue       # 작업 버튼들
└── common/
    ├── StatusBadge.vue       # 상태 배지
    ├── CategoryTag.vue       # 카테고리 태그
    ├── ProgressBar.vue       # 진행률 바
    └── NodeIcon.vue          # 레벨 아이콘
```

### 9.3 상태 관리 (Pinia)

```
stores/
├── project.ts         # 현재 프로젝트
├── wbs.ts             # WBS 트리 데이터
├── selection.ts       # 선택된 노드
└── settings.ts        # 전역 설정
```

---

## 10. 테마 시스템

### 10.1 Dark Blue 테마 (기본)

WBS 트리 목업을 기반으로 한 다크 블루 테마.

**색상 팔레트**

| 용도 | Hex |
|------|-----|
| 배경 (Background) | `#1a1a2e` |
| 헤더 (Header) | `#16213e` |
| 사이드바 (Sidebar) | `#0f0f23` |
| 카드 배경 (Cards) | `#1e1e38` |
| 프라이머리 (Primary) | `#3b82f6` |
| 성공 (Success) | `#22c55e` |
| 경고 (Warning) | `#f59e0b` |
| 위험 (Danger) | `#ef4444` |
| 텍스트 (Primary) | `#e8e8e8` |
| 텍스트 (Secondary) | `#888888` |
| 보더 (Border) | `#3d3d5c` |

**계층 아이콘 색상**

| 계층 | 색상 | Hex |
|------|------|-----|
| Project | 퍼플 | `#8b5cf6` |
| Work Package | 블루 | `#3b82f6` |
| Activity | 그린 | `#22c55e` |
| Task | 앰버 | `#f59e0b` |

---

## 11. 비기능 요구사항

### 11.1 성능

- 초기 로딩: < 2초
- 트리 렌더링: 1000 노드 기준 < 500ms
- 검색 반응: < 100ms

### 11.2 접근성

- 키보드 네비게이션 지원
- 스크린 리더 호환

### 11.3 호환성

- 브라우저: Chrome, Firefox, Safari, Edge (최신 2버전)
- Node.js: 20.x 이상

---

## 12. 상태 기호 요약

| 칸반 컬럼 | 상태 기호 | 의미 |
|-----------|-----------|------|
| Todo | `[ ]` | 대기 |
| Design | `[bd]` | 기본설계 |
| Detail | `[dd]`, `[an]` | 상세설계/분석 |
| Approve | `[ap]` | 설계승인 |
| Implement | `[im]`, `[fx]` | 구현/수정 |
| Verify | `[vf]` | 검증 |
| Done | `[xx]` | 완료 |

---

## 13. CLI Tools

### 13.1 개요

orchay은 웹 UI 외에도 CLI 도구를 제공하여 터미널에서 직접 워크플로우를 실행할 수 있습니다.

**핵심 목적**: Claude Code 등 LLM CLI의 context 제한 문제를 해결하기 위한 **외부 Orchestrator**

### 13.2 Workflow Orchestrator

#### 문제점
- `/wf:auto` 명령어 실행 시 여러 단계를 연속 실행하면 context가 누적되어 세션이 중단됨
- 각 단계(start, draft, build 등)마다 Subagent 호출 → 결과 누적 → context 폭발

#### 해결책
- Node.js 기반 외부 Orchestrator가 워크플로우를 관리
- 각 단계마다 **새로운 Claude 세션**을 생성하여 context 초기화
- 상태는 `wbs.md`와 `workflow-state.json`에 저장하여 재개 가능

### 13.3 CLI 명령어

```bash
# 워크플로우 자동 실행 (완료까지)
orchay workflow TSK-03-05

# 특정 단계까지만 실행
orchay workflow TSK-03-05 --until build

# 실행 계획만 확인
orchay workflow TSK-03-05 --dry-run

# 중단된 워크플로우 재개
orchay workflow TSK-03-05 --resume

# 상세 로그 출력
orchay workflow TSK-03-05 --verbose
```

### 13.4 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    orchay workflow CLI                       │
├─────────────────────────────────────────────────────────────┤
│  Node.js Orchestrator (상주 프로세스)                        │
│  ├── 1. wbs.md에서 현재 상태 확인                           │
│  ├── 2. 다음 단계 결정                                       │
│  ├── 3. Claude CLI 호출 (새 세션)                           │
│  │      └── claude -p "/wf:{command} {taskId}"              │
│  ├── 4. 완료 대기 및 결과 확인                               │
│  ├── 5. workflow-state.json 업데이트                        │
│  └── 6. 다음 단계로 반복 또는 종료                           │
└─────────────────────────────────────────────────────────────┘
```

### 13.5 파일 구조

```
orchay/
├── bin/
│   └── orchay.js             # CLI 진입점
├── cli/
│   ├── index.ts              # CLI 메인
│   ├── commands/
│   │   └── workflow.ts       # workflow 명령어
│   └── lib/
│       ├── runner.ts         # 워크플로우 러너
│       ├── state-manager.ts  # 상태 관리
│       ├── wbs-reader.ts     # wbs.md 파싱
│       ├── claude-executor.ts # Claude CLI 호출
│       └── workflow-config.ts # 워크플로우 정의
└── .orchay/
    └── projects/{project}/
        └── workflow-state.json  # 진행 상태 추적
```

### 13.6 workflow-state.json 스키마

```json
{
  "taskId": "TSK-03-05",
  "currentStep": "draft",
  "nextStep": "review",
  "target": "done",
  "status": "in_progress",
  "startedAt": "2025-12-15T10:00:00Z",
  "updatedAt": "2025-12-15T10:30:00Z",
  "completedSteps": [
    { "step": "start", "completedAt": "2025-12-15T10:15:00Z" },
    { "step": "draft", "completedAt": "2025-12-15T10:30:00Z" }
  ]
}
```

### 13.7 장점

| 항목 | 설명 |
|------|------|
| Context 초기화 | 각 단계마다 새 Claude 세션 → context 누적 없음 |
| 완전 자동 | 사용자 개입 없이 처음부터 끝까지 실행 |
| 재개 가능 | 중단 시 `--resume`으로 이어서 실행 |
| 가시성 | 각 단계 진행 상황이 터미널에 실시간 표시 |
| 유연성 | `--until`, `--dry-run` 등 옵션으로 세밀한 제어 |

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| 1.4 | 2025-12-18 | **워크플로우 스키마 v2.0 반영**: workflows.json이 Record 기반 v2.0 스키마로 변경됨 (states, commands, workflows 섹션 분리) |
| 1.3 | 2025-12-18 | 설계승인(approve) 단계 추가 - `[ap]` 상태, development 전용 (5.1, 5.2, 5.3, 12) |
| 1.2 | 2025-12-15 | Task 속성에 `completed` 필드 추가 (7.4, 7.5) |
| 1.1 | 2025-12-15 | CLI Tools 섹션 추가 (Workflow Orchestrator) |
| 1.0 | 2025-12-13 | 1차 범위 PRD 작성 (WBS 트리 뷰 중심) |
