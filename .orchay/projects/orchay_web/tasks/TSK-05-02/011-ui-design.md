# 화면설계 (011-ui-design.md)

**Template Version:** 3.0.0 — **Last Updated:** 2025-12-15

> **화면설계 규칙**
> * 실제 구현 가능한 **UI 레이아웃 중심**으로 작성
> * **SVG 개념도를 필수**로 포함 (ui-assets/ 폴더)
> * 기본설계와 **100% 일치**해야 함 (차이 발견 시 즉시 중단)
> * PrimeVue 4.x 컴포넌트를 **명시적으로 지정**
> * 접근성(ARIA), 반응형, 상태 관리를 **구체적으로** 명시

---

## 0. 문서 메타데이터

| 항목 | 내용 |
|------|------|
| Task ID | TSK-05-02 |
| Task명 | Detail Sections |
| Category | development |
| 상태 | [dd] 상세설계 |
| 작성일 | 2025-12-15 |
| 작성자 | AI Agent |

### 상위 문서 참조

| 문서 유형 | 경로 | 참조 섹션 |
|----------|------|----------|
| PRD | `.orchay/projects/orchay/prd.md` | 섹션 6.3, 9.2, 10.1 |
| 기본설계 | `010-basic-design.md` | 전체 |
| 상세설계 | `020-detail-design.md` | 섹션 9 (UI 설계) |

### 관련 SVG 파일

| 파일명 | 설명 |
|--------|------|
| `ui-assets/screen-01-workflow-development.svg` | development 워크플로우 흐름 |
| `ui-assets/screen-01-workflow-defect.svg` | defect 워크플로우 흐름 |
| `ui-assets/screen-02-requirements-view.svg` | 요구사항 목록 읽기 모드 |
| `ui-assets/screen-02-requirements-edit.svg` | 요구사항 편집 모드 |
| `ui-assets/screen-03-documents-list.svg` | 문서 목록 (존재/예정) |
| `ui-assets/screen-04-history-timeline.svg` | 이력 타임라인 |

---

## 1. 화면 개요

### 1.1 화면 식별

| 항목 | 내용 |
|------|------|
| 화면 ID | SCR-05-02 |
| 화면명 | Task 상세 섹션 (Workflow, Requirements, Documents, History) |
| 경로 | /wbs (우측 패널 내부) |
| 접근 권한 | 모든 사용자 (읽기), 편집 권한자 (요구사항 수정) |
| 부모 화면 | TaskDetailPanel.vue |

### 1.2 화면 목적

4개의 섹션 컴포넌트를 통해 Task의 전체 컨텍스트를 시각화:
- **TaskWorkflow**: 카테고리별 워크플로우 진행 단계 표시
- **TaskRequirements**: 요구사항 목록 및 인라인 편집
- **TaskDocuments**: 관련 문서 목록 및 뷰어 연동
- **TaskHistory**: 상태 변경 이력 타임라인

---

## 2. 화면 구조

### 2.1 전체 레이아웃 (TaskDetailPanel 내부)

```
┌─────────────────────────────────────────────────────┐
│ TaskDetailPanel (우측 패널)                         │
├─────────────────────────────────────────────────────┤
│ TaskBasicInfo (TSK-05-01)                           │
├─────────────────────────────────────────────────────┤
│ TaskProgress (TSK-05-01)                            │
├─────────────────────────────────────────────────────┤
│ TaskWorkflow (이 Task)                              │ ← 섹션 1
├─────────────────────────────────────────────────────┤
│ TaskRequirements (이 Task)                          │ ← 섹션 2
├─────────────────────────────────────────────────────┤
│ TaskDocuments (이 Task)                             │ ← 섹션 3
├─────────────────────────────────────────────────────┤
│ TaskHistory (이 Task)                               │ ← 섹션 4
└─────────────────────────────────────────────────────┘
```

### 2.2 컴포넌트 계층 구조

```
TaskDetailPanel.vue (TSK-05-01)
├── TaskBasicInfo.vue (TSK-05-01)
├── TaskProgress.vue (TSK-05-01)
├── TaskWorkflow.vue (이 Task)
│   └── (노드 반복 렌더링, 서브 컴포넌트 없음)
├── TaskRequirements.vue (이 Task)
│   └── (인라인 편집 UI)
├── TaskDocuments.vue (이 Task)
│   └── DocumentCard (PrimeVue Card 직접 사용)
└── TaskHistory.vue (이 Task)
    └── (PrimeVue Timeline 사용)
```

---

## 3. 섹션별 상세 설계

---

## 3.1 TaskWorkflow (워크플로우 흐름 시각화)

### 3.1.1 레이아웃 구조

**PrimeVue 컴포넌트**: Panel (헤더 + 내용)

```
┌─────────────────────────────────────────────────────┐
│ 워크플로우 흐름                            (Panel)  │
├─────────────────────────────────────────────────────┤
│                                                       │
│  ┌──────┐    ┌──────┐    ┌──────┐    ┌──────┐       │
│  │ Todo │ →  │Design│ →  │Detail│ →  │Implem│ → ... │
│  │ [ ]  │    │ [bd] │    │ [dd] │    │ [im] │       │
│  └──────┘    └──────┘    └──────┘    └──────┘       │
│   회색        회색        파란색       회색          │
│                          (현재)                      │
│                                                       │
└─────────────────────────────────────────────────────┘
```

**SVG 참조**:
- `screen-01-workflow-development.svg` (development 카테고리)
- `screen-01-workflow-defect.svg` (defect 카테고리)

### 3.1.2 UI 요소 명세

| 요소 | PrimeVue 컴포넌트 | 설명 |
|------|------------------|------|
| 컨테이너 | Panel | toggleable=false, 헤더 "워크플로우 흐름" |
| 노드 컨테이너 | div (flex) | justify-content: space-around, gap: 1rem |
| 노드 | div (커스텀) | 80px × 60px, 둥근 모서리 (rounded-lg) |
| 화살표 | i.pi.pi-arrow-right | 노드 사이에 삽입, 회색 (#9ca3af) |

### 3.1.3 상태별 스타일

| 상태 | 배경색 | 텍스트색 | 테두리 | 효과 |
|------|--------|---------|--------|------|
| 완료 (과거) | #22c55e (green-500) | #ffffff | 2px solid #22c55e | - |
| 현재 | #3b82f6 (blue-500) | #ffffff | 2px solid #3b82f6 | font-weight: 700, transform: scale(1.1) |
| 미완료 (미래) | #f9fafb (gray-50) | #6b7280 (gray-500) | 2px dashed #9ca3af | - |

### 3.1.4 접근성

| 속성 | 값 | 설명 |
|------|-----|------|
| role | "list" | 노드 컨테이너에 적용 |
| 각 노드 role | "listitem" | 각 노드 div에 적용 |
| aria-current | "step" | 현재 상태 노드에만 적용 |
| aria-label | "워크플로우 단계: {name} ({code})" | 각 노드에 적용 |

### 3.1.5 카테고리별 워크플로우 단계

**development (6단계)**:
```
[ ] Todo → [bd] Design → [dd] Detail → [im] Implement → [vf] Verify → [xx] Done
```

**defect (5단계)**:
```
[ ] Todo → [an] Analyze → [fx] Fix → [vf] Verify → [xx] Done
```

**infrastructure (4단계)**:
```
[ ] Todo → [ds] Design (선택) → [im] Implement → [xx] Done
```

### 3.1.6 데이터 흐름

**Props**:
```typescript
interface TaskWorkflowProps {
  task: TaskDetail // selectedTask from Pinia
}
```

**Computed**:
```typescript
// workflowSteps: WorkflowStep[]
// currentStepIndex: number
```

**Events**: 없음 (읽기 전용)

---

## 3.2 TaskRequirements (요구사항 관리)

### 3.2.1 레이아웃 구조 (읽기 모드)

```
┌─────────────────────────────────────────────────────┐
│ 요구사항                               [편집] (Panel)│
├─────────────────────────────────────────────────────┤
│ ref: PRD 6.3.2, 6.3.3, 6.3.4, 6.3.6                 │
├─────────────────────────────────────────────────────┤
│                                                       │
│ • TaskWorkflow 컴포넌트 (워크플로우 흐름 시각화)    │
│ • TaskRequirements 컴포넌트 (요구사항 목록 관리)    │
│ • TaskDocuments 컴포넌트 (문서 목록 표시)           │
│ • TaskHistory 컴포넌트 (상태 변경 이력 타임라인)    │
│                                                       │
└─────────────────────────────────────────────────────┘
```

**SVG 참조**: `screen-02-requirements-view.svg`

### 3.2.2 레이아웃 구조 (편집 모드)

```
┌─────────────────────────────────────────────────────┐
│ 요구사항                          [취소] [저장] (Panel)│
├─────────────────────────────────────────────────────┤
│                                                       │
│ ┌─────────────────────────────────────────────┐ [삭제]│
│ │ TaskWorkflow 컴포넌트 (워크플로우 흐름...)  │      │
│ └─────────────────────────────────────────────┘      │
│ ┌─────────────────────────────────────────────┐ [삭제]│
│ │ TaskRequirements 컴포넌트 (요구사항...)     │      │
│ └─────────────────────────────────────────────┘      │
│ ┌─────────────────────────────────────────────┐ [삭제]│
│ │ TaskDocuments 컴포넌트 (문서 목록...)       │      │
│ └─────────────────────────────────────────────┘      │
│                                                       │
│                                             [+ 추가]  │
└─────────────────────────────────────────────────────┘
```

**SVG 참조**: `screen-02-requirements-edit.svg`

### 3.2.3 UI 요소 명세

| 요소 | PrimeVue 컴포넌트 | 설명 |
|------|------------------|------|
| 컨테이너 | Panel | toggleable=false, 헤더 "요구사항" |
| 헤더 액션 (읽기) | Button | label="편집", icon="pi pi-pencil", size="small" |
| 헤더 액션 (편집) | ButtonGroup | "취소" (secondary), "저장" (primary) |
| PRD 참조 | div (text-sm) | 회색 텍스트, 링크 형태 |
| 요구사항 목록 (읽기) | ul | 불릿 포인트, padding-left: 1.5rem |
| 요구사항 항목 (편집) | InputText | v-model, full-width, maxlength=500 |
| 삭제 버튼 | Button | icon="pi pi-trash", severity="danger", size="small" |
| 추가 버튼 | Button | label="추가", icon="pi pi-plus", outlined |

### 3.2.4 상태 관리

**Local State**:
```typescript
const isEditing = ref(false)
const localRequirements = ref<string[]>([])
```

**Props**:
```typescript
interface TaskRequirementsProps {
  task: TaskDetail
}
```

**Events**:
```typescript
emit('update:requirements', localRequirements.value)
```

### 3.2.5 접근성

| 요소 | ARIA 속성 |
|------|----------|
| 편집 버튼 | aria-label="요구사항 편집" |
| 취소 버튼 | aria-label="편집 취소" |
| 저장 버튼 | aria-label="요구사항 저장" |
| InputText | aria-label="요구사항 {index+1}" |
| 추가 버튼 | aria-label="요구사항 추가" |
| 삭제 버튼 | aria-label="요구사항 {index+1} 삭제" |

### 3.2.6 유효성 검증

| 규칙 | 검증 시점 | 에러 메시지 |
|------|----------|------------|
| 빈 문자열 금지 | 저장 시 | "빈 요구사항은 저장할 수 없습니다" |
| 500자 제한 | 입력 중 | InputText maxlength로 차단 |
| 중복 항목 금지 | 저장 시 | "중복된 요구사항이 있습니다" |

---

## 3.3 TaskDocuments (문서 목록)

### 3.3.1 레이아웃 구조

```
┌─────────────────────────────────────────────────────┐
│ 관련 문서                                    (Panel) │
├─────────────────────────────────────────────────────┤
│                                                       │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 📄 010-basic-design.md                          │ │
│ │ 타입: 기본설계                                   │ │
│ │ 크기: 15.5 KB | 수정일: 2025-12-15 13:12  [열기]│ │
│ └─────────────────────────────────────────────────┘ │
│ (파란 배경 #DBEAFE)                                  │
│                                                       │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 📄 020-detail-design.md                         │ │
│ │ 타입: 상세설계                                   │ │
│ │ 생성 조건: /wf:draft 실행 후 생성                │ │
│ └─────────────────────────────────────────────────┘ │
│ (회색 배경 #F9FAFB, 점선 테두리)                     │
│                                                       │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 📄 030-implementation.md                        │ │
│ │ 타입: 구현                                       │ │
│ │ 생성 조건: /wf:build 실행 후 생성                │ │
│ └─────────────────────────────────────────────────┘ │
│ (회색 배경 #F9FAFB, 점선 테두리)                     │
│                                                       │
└─────────────────────────────────────────────────────┘
```

**SVG 참조**: `screen-03-documents-list.svg`

### 3.3.2 UI 요소 명세

| 요소 | PrimeVue 컴포넌트 | 설명 |
|------|------------------|------|
| 컨테이너 | Panel | toggleable=false, 헤더 "관련 문서" |
| 문서 카드 | Card | 각 문서마다 하나의 Card |
| 카드 헤더 | template #title | 아이콘 + 파일명 |
| 카드 내용 | template #content | 타입, 크기, 수정일 또는 생성 조건 |
| 카드 액션 | template #footer | "열기" 버튼 (exists=true일 때만) |
| 열기 버튼 | Button | label="열기", icon="pi pi-external-link", size="small" |

### 3.3.3 문서 타입별 아이콘 및 색상

| 타입 | 아이콘 | 색상 |
|------|--------|------|
| design | pi pi-file-edit | #3b82f6 (blue-500) |
| implementation | pi pi-code | #22c55e (green-500) |
| test | pi pi-check-square | #f59e0b (amber-500) |
| manual | pi pi-book | #8b5cf6 (purple-500) |
| analysis | pi pi-search | #ef4444 (red-500) |
| review | pi pi-comments | #06b6d4 (cyan-500) |

### 3.3.4 존재/예정 상태 스타일

| 상태 | 배경색 | 테두리 | 클릭 |
|------|--------|--------|------|
| 존재 (exists=true) | #DBEAFE (blue-100) | 2px solid #3B82F6 (blue-500) | 가능 (cursor: pointer) |
| 예정 (exists=false) | #F9FAFB (gray-50) | 2px dashed #9CA3AF (gray-400) | 불가 (cursor: not-allowed) |

### 3.3.5 접근성

| 요소 | ARIA 속성 |
|------|----------|
| 카드 (exists=true) | role="button", tabindex="0", aria-label="문서 열기: {name}" |
| 카드 (exists=false) | aria-disabled="true", aria-label="문서 예정: {name}" |
| 열기 버튼 | aria-label="문서 열기: {name}" |

### 3.3.6 데이터 흐름

**Props**:
```typescript
interface TaskDocumentsProps {
  documents: DocumentInfo[]
}
```

**Events**:
```typescript
emit('open-document', document: DocumentInfo)
```

### 3.3.7 상호작용

1. **카드 클릭** (exists=true): emit('open-document')
2. **카드 클릭** (exists=false): 아무 동작 없음
3. **열기 버튼 클릭**: emit('open-document')
4. **키보드 네비게이션**: Enter/Space로 문서 열기

---

## 3.4 TaskHistory (이력 타임라인)

### 3.4.1 레이아웃 구조

```
┌─────────────────────────────────────────────────────┐
│ 이력                                         (Panel) │
├─────────────────────────────────────────────────────┤
│                                                       │
│ ● 2025-12-15 13:12 - 상태 전이                      │
│   [bd] → [dd]                                        │
│   명령어: /wf:draft                                  │
│   문서 생성: 020-detail-design.md                    │
│   사용자: AI Agent                                   │
│                                                       │
│ ● 2025-12-15 09:30 - 상태 전이                      │
│   [ ] → [bd]                                         │
│   명령어: /wf:start                                  │
│   문서 생성: 010-basic-design.md                     │
│   사용자: AI Agent                                   │
│                                                       │
│ ● 2025-12-15 09:00 - Task 생성                      │
│   초기 상태: [ ]                                     │
│                                                       │
└─────────────────────────────────────────────────────┘
```

**SVG 참조**: `screen-04-history-timeline.svg`

### 3.4.2 UI 요소 명세

| 요소 | PrimeVue 컴포넌트 | 설명 |
|------|------------------|------|
| 컨테이너 | Panel | toggleable=false, 헤더 "이력" |
| 타임라인 | Timeline | align="left", layout="vertical" |
| 마커 | template #marker | 둥근 점 (●), 색상: action별 |
| 내용 | template #content | 타임스탬프, 액션, 상세 정보 |

### 3.4.3 액션 타입별 마커 색상

| 액션 타입 | 마커 색상 | 설명 |
|----------|----------|------|
| transition | #3b82f6 (blue-500) | 상태 전이 |
| action | #22c55e (green-500) | 액션 실행 (예: /wf:skip) |
| update | #f59e0b (amber-500) | Task 정보 수정 |

### 3.4.4 타임스탬프 포맷

| 필드 | 포맷 | 예시 |
|------|------|------|
| timestamp | YYYY-MM-DD HH:mm | 2025-12-15 13:12 |

**구현**:
```typescript
import { format } from 'date-fns'
const formattedTime = format(new Date(entry.timestamp), 'yyyy-MM-dd HH:mm')
```

### 3.4.5 이력 엔트리 표시 내용

**상태 전이 (action='transition')**:
- 타임스탬프
- "상태 전이" 레이블
- {previousStatus} → {newStatus}
- 명령어 (command)
- 생성된 문서 (documentCreated)
- 사용자 (userId)

**액션 실행 (action='action')**:
- 타임스탬프
- "액션 실행" 레이블
- 명령어 (command)
- 코멘트 (comment)
- 사용자 (userId)

**Task 수정 (action='update')**:
- 타임스탬프
- "Task 수정" 레이블
- 변경 내용 (comment)
- 사용자 (userId)

### 3.4.6 접근성

| 요소 | ARIA 속성 |
|------|----------|
| 타임라인 아이템 | aria-label="이력: {action} {timestamp}" |
| 타임라인 전체 | role="list" (PrimeVue 기본 제공) |

### 3.4.7 데이터 흐름

**Props**:
```typescript
interface TaskHistoryProps {
  history: HistoryEntry[]
}
```

**Computed**:
```typescript
// sortedHistory: HistoryEntry[] (타임스탬프 역순)
const sortedHistory = computed(() =>
  [...props.history].sort((a, b) =>
    new Date(b.timestamp).getTime() - new Date(a.timestamp).getTime()
  )
)
```

**Events**: 없음 (읽기 전용)

---

## 4. 통합 (TaskDetailPanel)

### 4.1 이벤트 핸들링

**요구사항 수정 핸들러**:
```typescript
async function handleUpdateRequirements(newRequirements: string[]) {
  const previousRequirements = selectedTask.value?.requirements

  // 낙관적 업데이트
  if (selectedTask.value) {
    selectedTask.value.requirements = newRequirements
  }

  try {
    await $fetch(`/api/tasks/${selectedTask.value?.id}`, {
      method: 'PUT',
      body: { requirements: newRequirements }
    })

    toast.add({ severity: 'success', summary: '요구사항이 저장되었습니다' })
    await refreshTaskDetail()
  } catch (error) {
    // 롤백
    if (selectedTask.value && previousRequirements) {
      selectedTask.value.requirements = previousRequirements
    }
    toast.add({ severity: 'error', summary: '저장 실패', detail: error.message })
  }
}
```

**문서 열기 핸들러**:
```typescript
function handleOpenDocument(document: DocumentInfo) {
  const documentViewerStore = useDocumentViewerStore()
  documentViewerStore.openDocument(document)
}
```

### 4.2 컴포넌트 배치 순서

1. TaskBasicInfo (TSK-05-01)
2. TaskProgress (TSK-05-01)
3. **TaskWorkflow** (이 Task)
4. **TaskRequirements** (이 Task)
5. **TaskDocuments** (이 Task)
6. **TaskHistory** (이 Task)

### 4.3 Props 전달

```vue
<TaskWorkflow :task="selectedTask" />
<TaskRequirements
  :task="selectedTask"
  @update:requirements="handleUpdateRequirements"
/>
<TaskDocuments
  :documents="selectedTask.documents"
  @open-document="handleOpenDocument"
/>
<TaskHistory :history="selectedTask.history" />
```

---

## 5. 반응형 디자인

### 5.1 브레이크포인트

| 구분 | 너비 | 지원 여부 |
|------|------|----------|
| Desktop | ≥ 1200px | 지원 (기본) |
| Tablet | 768-1199px | 미지원 (PRD 제약) |
| Mobile | < 768px | 미지원 (PRD 제약) |

### 5.2 데스크톱 레이아웃 (≥ 1200px)

- 워크플로우 노드: 수평 배치, overflow-x: auto (단계 많을 시)
- 요구사항/문서/이력: 전체 너비
- 카드 간격: gap: 1rem

---

## 6. 상태 관리

### 6.1 Pinia Store 연동

| Store | 사용 위치 | 목적 |
|-------|----------|------|
| selectionStore | TaskDetailPanel | selectedTask 읽기 |
| documentViewerStore | TaskDetailPanel | openDocument 액션 |

### 6.2 로컬 상태

| 컴포넌트 | 로컬 상태 | 목적 |
|----------|----------|------|
| TaskRequirements | isEditing, localRequirements | 편집 모드 관리 및 롤백 |
| TaskWorkflow | 없음 | 읽기 전용 |
| TaskDocuments | 없음 | Props만 렌더링 |
| TaskHistory | 없음 | 읽기 전용 |

---

## 7. 접근성 요약

### 7.1 키보드 네비게이션

| 컴포넌트 | 키보드 지원 |
|----------|------------|
| TaskWorkflow | Tab으로 읽기 (상호작용 없음) |
| TaskRequirements | Tab으로 Input 이동, Enter로 저장, Esc로 취소 |
| TaskDocuments | Tab으로 카드 이동, Enter/Space로 열기 |
| TaskHistory | Tab으로 읽기 (상호작용 없음) |

### 7.2 스크린리더 지원

- 모든 버튼: aria-label 명시
- 워크플로우 노드: aria-current="step" (현재 상태)
- 문서 카드: role="button", aria-disabled (예정 문서)
- 타임라인: PrimeVue Timeline 기본 접근성 활용

---

## 8. 성능 고려사항

### 8.1 렌더링 최적화

- **v-for key**: 고유한 ID 사용 (요구사항 index, 문서 path, 이력 timestamp)
- **computed 캐싱**: workflowSteps, sortedHistory는 computed로 캐싱
- **이벤트 throttle**: 요구사항 편집 시 입력 이벤트 throttle (300ms)

### 8.2 데이터 로딩

- 이력 100개 이상: 가상 스크롤 검토 (향후)
- 문서 목록: 정적 데이터이므로 추가 로딩 없음

---

## 9. 에러 처리 UI

### 9.1 토스트 메시지

| 상황 | severity | summary | detail |
|------|----------|---------|--------|
| 요구사항 저장 성공 | success | 요구사항이 저장되었습니다 | - |
| 요구사항 저장 실패 | error | 저장 실패 | {error.message} |
| 유효성 검증 실패 | warn | 입력 오류 | 빈 요구사항은 저장할 수 없습니다 |

### 9.2 빈 상태 UI

| 컴포넌트 | 빈 상태 메시지 |
|----------|---------------|
| TaskRequirements | "요구사항이 없습니다. 추가 버튼을 눌러 추가하세요." |
| TaskDocuments | "관련 문서가 없습니다." |
| TaskHistory | "이력이 없습니다." |

---

## 10. 테스트 시나리오 (UI 중심)

### 10.1 TaskWorkflow

| 시나리오 | 테스트 방법 | 기대 결과 |
|---------|------------|----------|
| development 워크플로우 표시 | category='development' Task 선택 | 6단계 노드 표시 |
| 현재 상태 강조 | status='[dd]' Task 선택 | Detail 노드만 파란색 + 볼드 |
| 완료 상태 표시 | status='[dd]' Task 선택 | Todo, Design 노드 초록색 |
| defect 워크플로우 표시 | category='defect' Task 선택 | 5단계 노드 표시 |

### 10.2 TaskRequirements

| 시나리오 | 테스트 방법 | 기대 결과 |
|---------|------------|----------|
| 읽기 모드 표시 | 컴포넌트 마운트 | 불릿 포인트로 목록 표시 |
| 편집 모드 전환 | 편집 버튼 클릭 | InputText로 전환 |
| 요구사항 추가 | 추가 버튼 클릭 | 빈 입력 필드 추가 |
| 요구사항 삭제 | 삭제 버튼 클릭 | 해당 항목 제거 |
| 저장 성공 | 저장 버튼 클릭 (유효한 데이터) | API 호출 + 성공 토스트 |
| 저장 실패 (빈 항목) | 저장 버튼 클릭 (빈 문자열) | 경고 토스트 |

### 10.3 TaskDocuments

| 시나리오 | 테스트 방법 | 기대 결과 |
|---------|------------|----------|
| 존재하는 문서 표시 | exists=true 문서 | 파란 배경, 열기 버튼 |
| 예정 문서 표시 | exists=false 문서 | 회색 배경, 점선 테두리 |
| 문서 열기 | 존재하는 문서 클릭 | open-document 이벤트 발행 |
| 예정 문서 클릭 차단 | 예정 문서 클릭 | 아무 동작 없음 |

### 10.4 TaskHistory

| 시나리오 | 테스트 방법 | 기대 결과 |
|---------|------------|----------|
| 이력 역순 정렬 | history 배열 전달 | 최신 이력이 위에 |
| 상태 전이 표시 | action='transition' | previousStatus → newStatus 표시 |
| 타임스탬프 포맷 | 이력 렌더링 | YYYY-MM-DD HH:mm 형식 |
| 빈 이력 | history=[] | "이력이 없습니다" 메시지 |

---

## 11. 구현 체크리스트

### Frontend
- [ ] TaskWorkflow.vue 구현
  - [ ] workflowSteps computed (category별 분기)
  - [ ] currentStepIndex computed
  - [ ] 노드 렌더링 및 스타일
  - [ ] 접근성 (ARIA)
- [ ] TaskRequirements.vue 구현
  - [ ] 읽기/편집 모드 토글
  - [ ] 요구사항 추가/삭제/수정
  - [ ] update:requirements 이벤트
  - [ ] 유효성 검증
- [ ] TaskDocuments.vue 구현
  - [ ] 문서 카드 렌더링
  - [ ] 존재/예정 스타일 구분
  - [ ] open-document 이벤트
  - [ ] 접근성 (role, aria-disabled)
- [ ] TaskHistory.vue 구현
  - [ ] PrimeVue Timeline 사용
  - [ ] 이력 역순 정렬
  - [ ] 타임스탬프 포맷팅
  - [ ] 액션 타입별 마커 색상
- [ ] TaskDetailPanel 통합
  - [ ] 4개 컴포넌트 배치
  - [ ] handleUpdateRequirements 핸들러
  - [ ] handleOpenDocument 핸들러
  - [ ] 에러 핸들링 (토스트)

### SVG 파일
- [ ] screen-01-workflow-development.svg
- [ ] screen-01-workflow-defect.svg
- [ ] screen-02-requirements-view.svg
- [ ] screen-02-requirements-edit.svg
- [ ] screen-03-documents-list.svg
- [ ] screen-04-history-timeline.svg

### 품질
- [ ] 단위 테스트 (computed, 이벤트)
- [ ] E2E 테스트 (Playwright)
- [ ] 접근성 테스트 (키보드, 스크린리더)
- [ ] 반응형 테스트 (Desktop)

---

## 관련 문서

- 기본설계: `010-basic-design.md`
- 상세설계: `020-detail-design.md`
- 추적성 매트릭스: `025-traceability-matrix.md`
- 테스트 명세: `026-test-specification.md`
- PRD: `.orchay/projects/orchay/prd.md`

---

<!--
author: AI Agent
Template Version: 3.0.0
-->
